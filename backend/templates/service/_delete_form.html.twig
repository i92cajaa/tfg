<form method="post" action="{{ path('service_delete', {'service': service.id}) }}" class="deleteForm">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
    <button class="btn btn-grey">Borrar</button>
</form>
