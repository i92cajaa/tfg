{% extends 'base-admin.html.twig' %}

{% block title %}{{ 'Tipos de Mentorías'|trans }}{% endblock %}

{% block page_title %}
    {{ 'List Services'|trans }}
{% endblock %}

{% block body %}
<div class="container-fluid">
    <!-- list section start -->
    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-datatable overflow-inherit table-responsive card-body">
                    <div class="overflow-auto">
                        <table class="user-list-table table">
                            <thead>
                                <tr>
                                    <th>
                                        <a href="{{ filterService.orderBy("name", filterService.getInversedOrder("name")) }}">
                                            {{ 'Name'|trans }} {% if filterService.isOrdered("name") %}<i data-feather="{{ filterService.getOrder("name").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}
                                        </a>
                                    </th>
                                    <!--<th>
                                        <a href="{{ filterService.orderBy("division", filterService.getInversedOrder("division")) }}">
                                            {{ 'Division'|trans }} {% if filterService.isOrdered("division") %}<i data-feather="{{ filterService.getOrder("division").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}
                                        </a>
                                    </th>-->
    {#                                <th>#}
    {#                                    <a href="{{ filterService.orderBy("user", filterService.getInversedOrder("user")) }}">#}
    {#                                        {{ (configuration.user_nomenclature ~ 's')|trans }} {% if filterService.isOrdered("user") %}<i data-feather="{{ filterService.getOrder("user").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}#}
    {#                                    </a>#}
    {#                                </th>#}
                                    <th>
                                        <a href="{{ filterService.orderBy("color", filterService.getInversedOrder("color")) }}">
                                            {{ 'Color'|trans }} {% if filterService.isOrdered("color") %}<i data-feather="{{ filterService.getOrder("color").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}
                                        </a>
                                    </th>
                                    {% if configuration.invoice %}
                                        <th>
                                            <a href="{{ filterService.orderBy("iva", filterService.getInversedOrder("iva")) }}">
                                                {{ 'VAT'|trans }} {% if filterService.isOrdered("iva") %}<i data-feather="{{ filterService.getOrder("iva").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}
                                            </a>
                                        </th>
                                    {% endif %}
    {#                                <th>#}
    {#                                    <a href="{{ filterService.orderBy("price", filterService.getInversedOrder("price")) }}">#}
    {#                                        {{ 'Price'|trans }} {% if filterService.isOrdered("price") %}<i data-feather="{{ filterService.getOrder("price").order == 'asc' ? 'arrow-up' : 'arrow-down' }}"></i>{% endif %}#}
    {#                                    </a>#}
    {#                                </th>#}
                                    <th>
                                        <a>
                                            {{ 'Actions'|trans }}
                                        </a>
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for service in services['data'] %}
                                <tr>
                                    <td>{{ service.name }}</td>
                                    <!--<td>
                                        {% if service.division is not null %}{{ service.division.name }}{% endif %}
                                    </td>-->
    {#                                <td>#}
    {#                                    {% for user in service.professionals %}#}
    {#                                            <a class="" href="{{ path('user_show', {'user':user.id }) }}"><span class="badge badge-light-secondary">{{ user.name }} {{ user.surnames }}</span></a>#}
    {#                                        {% else %}#}

    {#                                    {% endfor %}#}
    {#                                </td>#}
                                    <td>
                                        <div class="d-inline-flex">
                                            <div class="input-color p-t-5">
                                                <div style="background-color: {{ service.color }}; width: 10px; height:10px;"></div>
                                            </div>
                                            <div class="ml-1 m-r-5">
                                                {{ service.color }}
                                            </div>
                                        </div>
                                    </td>
                                    {% if configuration.invoice %}
                                        <td>
                                            {{ service.iva|number_format(2) }}%
                                        </td>
                                    {% endif %}
    {#                                <td>{{ service.price|number_format(2) }}€</td>#}
                                    <td>
                                        <div class="dropdown-basic ">
                                            <div class="dropdown">
                                                <button class="dropbtn bg-transparent text-dark p-0 m-0" type="button" data-bs-original-title="" title="">
                                                    <i data-feather='align-justify'></i>
                                                </button>
                                                <div class="dropdown-content " style="top: 20px; left: -150px">
                                                    {% if userPermission.can('show', 'services') %}
                                                        <a class="dropdown-item" href="{{ path('service_show', {'service': service.id}) }}">
                                                            <i class="align-middle" data-feather='eye'></i>
                                                            <span class="align-middle"> {{ 'Show'|trans }}</span></a>
                                                    {% endif %}
                                                    {% if userPermission.can('edit', 'services') %}
                                                        <a class="dropdown-item" href="{{ path('service_edit', {'service': service.id}) }}">
                                                            <i class="align-middle" data-feather='edit'></i>
                                                            <span class="align-middle"> {{ 'Edit'|trans }}</span></a>
                                                    {% endif %}
                                                    {% if userPermission.can('delete', 'services') %}
                                                    <div class="dropdown-divider"></div>
                                                        <a class="dropdown-item" onclick="$('#deleteForm_service_{{ service.id }}').submit()">
                                                            <i class="align-middle" data-feather='delete'></i>
                                                            <span class="align-middle"> {{ 'Delete'|trans }}</span></a>
                                                    <form method="post" id="deleteForm_service_{{ service.id }}" class="d-none deleteForm" action="{{ path('service_delete', {'service': service.id}) }}" >
                                                        <input type="hidden" name="_method" value="DELETE">
                                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
                                                    </form>
                                                    {% endif %}
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="{% if configuration.invoice %}7{% else %}6{% endif %}">{{ 'No results found'|trans }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                    <div class="d-flex justify-content-end mr-4 mt-2">
                        {% include 'extra/pagination.html.twig' with {
                            filterService: filterService,
                            currentPage: filterService.page,
                            paginationPath: 'service_index',
                            lastPage: lastPage,
                            showAlwaysFirstAndLast: true
                        } only %}
                    </div>
                    {% include 'extra/filter_limit.html.twig' %}
                </div>
            </div>
        </div>

        <!--{% if userPermission.can('manage_divisions', 'services') %}
            <div class="col-md-3">
                <div class="card">
                    <div class="card-datatable overflow-inherit table-responsive card-body">
                        <table class="user-list-table table">
                            <thead>
                            <tr>
                                <th>{{ 'Division'|trans }}</th>
                                <th class="text-center">{{ 'Actions'|trans }}</th>
                            </tr>
                            </thead>
                            <tbody>
                            {% for division in divisions %}
                                <tr>
                                    <td>{{ division.name }}</td>
                                    <td>
                                        <div class="dropdown-basic ">
                                            <div class="dropdown">
                                                <button class="dropbtn bg-transparent text-dark p-0 m-0" type="button" data-bs-original-title="" title="">
                                                    <i data-feather='align-justify'></i>
                                                </button>
                                                <div class="dropdown-content " style="top: 20px">
                                                    <a class="dropdown-item" href="{{ path('division_edit', {'division': division.id}) }}"><i data-feather='edit'></i> {{ 'Edit'|trans }}</a>
                                                    <div class="dropdown-divider"></div>
                                                    <a class="dropdown-item" onclick="$('#deleteForm_division_{{ division.id }}').submit()"><i data-feather='delete'></i> {{ 'Delete'|trans }}</a>
                                                    <form method="post" id="deleteForm_division_{{ division.id }}" class="deleteForm d-none" action="{{ path('division_delete', {'division': division.id}) }}">
                                                        <input type="hidden" name="_method" value="DELETE">
                                                        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ division.id) }}">
                                                    </form>
                                                </div>
                                            </div>
                                        </div>
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="2">{{ 'No results found'|trans }}</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                            <tfoot class="bg-transparent">
                            <tr class="bg-transparent">
                                <td></td>
                                <td class="text-end bg-transparent"><a href="{{ path('division_new') }}"><button class="btn add-new btn-primary" type="button"><span class="text-white">{{ 'Create Division'|trans }}</span></button></a></td>
                            </tr>
                            </tfoot>
                        </table>
                    </div>
                </div>
            </div>
        {% endif %}-->
    </div>
</div>

{% endblock %}

{% block action_buttons %}

    <li class="level-menu outside">
        {% if userPermission.can('create', 'services') %}
            <a class="nav-link" href="{{ path('service_new') }}" >
                <i data-feather="plus-circle"></i>
            </a>
        {% endif %}

        <a class="nav-link cursor-pointer mr-1" href="#" onclick="toggleFilter()" >
            <i data-feather="filter"></i>
        </a>
    </li>

{% endblock %}

{% block sidebar %}
    {% include 'extra/filters/service_filter.html.twig' %}
{% endblock %}
