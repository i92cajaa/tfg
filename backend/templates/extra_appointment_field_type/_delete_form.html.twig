<form method="post" action="{{ path('extra_appointment_field_type_delete', {'extraAppointmentFieldType': extra_appointment_field_type.id}) }}" class="deleteForm">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ extra_appointment_field_type.id) }}">
    <button class="btn btn-outline-danger">{{ 'Delete'|trans }}</button>
</form>
