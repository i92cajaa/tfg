<form method="post" action="{{ path('festive_delete', {'festive': festive.id}) }}" class="deleteForm">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ festive.id) }}">
    <button class="btn btn-grey">{{ 'Delete'|trans }}</button>
</form>
