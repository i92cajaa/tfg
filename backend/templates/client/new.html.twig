{% extends 'base-admin.html.twig' %}

{% block title %}Crear Proyecto{% endblock %}

{% block body %}
<div class="container-fluid">

    <div class="card">
        <div class="card-body">
            <div class="tab-content">
                <!-- Account Tab starts -->
                <div class="tab-pane active" id="account" aria-labelledby="account-tab" role="tabpanel">
                    {{ include('client/_form.html.twig', {'button_label': 'Create'|trans}) }}
                </div>
                <!-- Account Tab ends -->
            </div>
        </div>
    </div>
</div>
{% endblock %}

{% block javascripts %}

{% endblock %}

{% block action_buttons %}
    <li class="level-menu outside d-flex align-items-center">

        <a class="nav-link cursor-pointer bg-primary-color" href="{{ userPermission.can('list', 'clients') ? path('client_index') : '#' }}">
            <i class="ficon" data-feather="chevron-left" style="stroke: white"></i>
        </a>
            <h5 class="ms-3 mb-0">Crear Proyecto</h5>

    </li>
{% endblock %}