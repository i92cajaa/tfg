<form method="post" class="deleteForm" action="{{ path('invoice_delete', {'invoice': invoice.id}) }}" >
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invoice.id) }}">
    <button class="btn btn-grey">{{ 'Delete'|trans }}</button>
</form>
