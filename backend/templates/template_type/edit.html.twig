{% extends 'base-admin.html.twig' %}

{% block title %}{{ 'Edit Template Type'|trans }}{% endblock %}

{% block page_title %}
    {{ 'Edit Template Type'|trans }}
{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="card">
            <div class="card-body">
                <div class="tab-content">
                    <!-- Account Tab starts -->
                    <div class="tab-pane active" id="account" aria-labelledby="account-tab" role="tabpanel">
                        {{ include('template_type/_form.html.twig', {'button_label': 'Update'|trans}) }}

                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    <script>

        $(function() {


        });
    </script>
{% endblock %}

{% block action_buttons %}
    <li class="level-menu outside">

        <a class="nav-link cursor-pointer" href="{{ userPermission.can('list', 'template_types') ? path('template_type_index') : '#' }}"  >
            <i class="ficon" data-feather="chevron-left"></i>
        </a>
    </li>
{% endblock %}

